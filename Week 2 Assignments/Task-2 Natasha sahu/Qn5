#include<iostream>
using namespace std;
int main(){
    int n,i;
    cout <<"Enter the size of the 1st array"<<endl;
    cin >> n;
    int a[n];
    cout <<"Enter the elements of the 1st array" <<endl;
    for(i=0;i<n;i++)
    cin >>a[i];
    cout <<"The elements of the 1st array" <<endl;
    for(i=0;i<n;i++)
    cout <<a[i] <<" ";
    int min=a[1],secmin=a[i];
    for(i=0;i<n;i++){
        if(a[i]<min){
            secmin=min;
            min=a[i];
        }
else if(a[i]!=min && a[i]<secmin){
    secmin=a[i];
}
    }


cout <<secmin <<" is the second smallest element";
}